import "server-only";
import { inFilter, supabaseSelect } from "@/lib/supabase-rest";

export async function getPublicCollectionBySlug(slug:string){const rows=await supabaseSelect<any>("Collection",{select:"*",slug:`eq.${slug}`,isPublished:"eq.true",limit:"1"});const collection=rows[0];if(!collection)return null;const links=await supabaseSelect<any>("CollectionProduct",{select:"productId,sortOrder",collectionId:`eq.${collection.id}`,order:"sortOrder.asc"});const ids=links.map(x=>x.productId).filter(Boolean);if(!ids.length)return{collection,products:[]};const products=await supabaseSelect<any>("Product",{select:"*",id:inFilter(ids),isPublished:"eq.true"});const images=await supabaseSelect<any>("Media",{select:"*",productId:inFilter(ids),order:"sortOrder.asc"});const brands=[...new Set(products.map(p=>p.brandId).filter(Boolean))];const brandRows=brands.length?await supabaseSelect<any>("Brand",{select:"id,name,slug,logoUrl",id:inFilter(brands),isPublished:"eq.true"}):[];const byId=new Map(products.map(p=>[p.id,p])),byBrand=new Map(brandRows.map(b=>[b.id,b])),byImages=new Map<string,any[]>();for(const i of images){const a=byImages.get(i.productId)||[];a.push(i);byImages.set(i.productId,a)}return{collection,products:ids.map(id=>byId.get(id)).filter(Boolean).map((p:any)=>({...p,images:byImages.get(p.id)||[],brandEntity:p.brandId?byBrand.get(p.brandId)||null:null}))};}

export async function getPublicProductAttributes(productId:string){const values=await supabaseSelect<any>("ProductAttributeValue",{select:"*",productId:`eq.${productId}`});if(!values.length)return[];const defs=await supabaseSelect<any>("ProductAttributeDefinition",{select:"*",id:inFilter(values.map(v=>v.definitionId)),isPublished:"eq.true",order:"sortOrder.asc"});const by=new Map(values.map(v=>[v.definitionId,v]));return defs.map(d=>({definition:d,value:by.get(d.id)}));}
