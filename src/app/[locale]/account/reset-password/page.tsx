import Link from "next/link";
import { KeyRound, ShieldCheck } from "lucide-react";
import { resetPasswordAction } from "../../actions";

export default async function ResetPasswordPage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{token?:string;error?:string}>}){
  const {locale}=await params; const q=await searchParams; const token=q.token??""; const action=resetPasswordAction.bind(null,locale);
  return <main className="py-16"><div className="mx-auto max-w-xl px-4"><section className="vitalis-glass rounded-[2rem] border border-white/70 p-7 shadow-xl sm:p-10"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><KeyRound className="h-7 w-7"/></div><h1 className="mt-6 text-2xl font-black text-foreground">تعیین رمز عبور جدید</h1><p className="mt-2 text-sm leading-7 text-muted">رمز جدید باید حداقل ۸ کاراکتر باشد. بعد از تغییر رمز، همه نشست‌های قبلی حساب بسته می‌شوند.</p>{q.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-xs font-bold text-red-700">{q.error==="mismatch"?"رمز و تکرار آن یکسان نیست.":"لینک بازیابی نامعتبر یا منقضی شده است."}</div>:null}{!token?<div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">لینک بازیابی معتبر در دسترس نیست. یک درخواست جدید ثبت کنید.</div>:<form action={action} className="mt-6 space-y-4"><input type="hidden" name="token" value={token}/><label className="block"><span className="mb-1.5 block text-xs font-bold text-muted">رمز جدید</span><input name="password" type="password" minLength={8} required autoComplete="new-password" className="h-12 w-full rounded-xl border border-border bg-white px-3 text-sm outline-none focus:border-primary" dir="ltr"/></label><label className="block"><span className="mb-1.5 block text-xs font-bold text-muted">تکرار رمز جدید</span><input name="confirmPassword" type="password" minLength={8} required autoComplete="new-password" className="h-12 w-full rounded-xl border border-border bg-white px-3 text-sm outline-none focus:border-primary" dir="ltr"/></label><button className="flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-primary px-5 text-sm font-black text-white"><ShieldCheck className="h-4 w-4"/>ثبت رمز جدید</button></form>}<div className="mt-6 text-center"><Link href={`/${locale}/account/forgot-password`} className="text-xs font-bold text-primary">درخواست لینک جدید</Link></div></section></div></main>;
}
