import Link from "next/link";
import { KeyRound, MailCheck } from "lucide-react";
import { forgotPasswordAction } from "../actions";

export default async function ForgotPasswordPage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{status?:string}>}){
  const {locale}=await params; const q=await searchParams; const action=forgotPasswordAction.bind(null,locale);
  return <main className="py-16"><div className="mx-auto max-w-xl px-4"><section className="vitalis-glass rounded-[2rem] border border-white/70 p-7 shadow-xl sm:p-10"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-primary/10 text-primary">{q.status?<MailCheck className="h-7 w-7"/>:<KeyRound className="h-7 w-7"/>}</div><h1 className="mt-6 text-2xl font-black text-foreground">بازیابی رمز عبور</h1><p className="mt-2 text-sm leading-7 text-muted">ایمیل یا شماره موبایل حساب را وارد کنید. برای حساب‌های دارای ایمیل، لینک امن ۳۰ دقیقه‌ای ارسال می‌شود.</p>{q.status==="sent"?<div className="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm leading-7 text-emerald-800">اگر حسابی با این اطلاعات وجود داشته باشد، راهنمای بازیابی برای آن ارسال شده است.</div>:q.status==="support"?<div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-7 text-amber-800">ارسال ایمیل بازیابی روی سرور هنوز پیکربندی نشده است. لطفاً از مرکز پشتیبانی درخواست بازیابی حساب ثبت کنید.</div>:<form action={action} className="mt-6 space-y-4"><label className="block"><span className="mb-1.5 block text-xs font-bold text-muted">ایمیل یا شماره موبایل</span><input name="identifier" required className="h-12 w-full rounded-xl border border-border bg-white px-3 text-sm outline-none focus:border-primary" dir="ltr"/></label><button className="min-h-12 w-full rounded-xl bg-primary px-5 text-sm font-black text-white">ارسال لینک بازیابی</button></form>}<div className="mt-6 flex justify-between text-xs font-bold"><Link href={`/${locale}/account/login`} className="text-primary">بازگشت به ورود</Link><Link href={`/${locale}/support`} className="text-muted">مرکز پشتیبانی</Link></div></section></div></main>;
}
