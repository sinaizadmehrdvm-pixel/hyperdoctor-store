import { notFound } from "next/navigation";
import { getLocale, getTranslations } from "next-intl/server";
import { Link } from "@/i18n/navigation";
import { Container } from "@/components/ui/container";
import { ProductGallery } from "@/components/site/product-gallery";
import { AddToCartButton } from "@/components/site/add-to-cart-button";
import { ProductActions } from "@/components/site/product-actions";
import { ShopProductCard } from "@/components/site/shop-product-card";
import { formatPrice } from "@/lib/utils";
import { localizedDescription, localizedName, pickLocalized } from "@/lib/i18n-content";
import { getProductBySlug, getRelatedProducts } from "@/lib/queries";
import { BadgeCheck, CheckCircle2, ChevronLeft, ChevronRight, Droplets, Headphones, Star, Volume2, Weight } from "lucide-react";

type LocalizedSpec = string | { fa?: string; tr?: string; en?: string; ar?: string };
type Specs = Record<string, LocalizedSpec>;
type ProductReview = { id: string; authorName: string; rating: number; title?: string | null; body?: string | null };
function l(locale:string,fa:string,en:string,tr:string,ar:string){if(locale==="en")return en;if(locale==="tr")return tr;if(locale==="ar")return ar;return fa;}
function localizedSpecValue(locale:string,value:LocalizedSpec){return typeof value==="string"?value:pickLocalized(locale,value);}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){
 const {slug}=await params;if(!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug))notFound();const[locale,t,c]=await Promise.all([getLocale(),getTranslations("shop"),getTranslations("common")]);const product=await getProductBySlug(slug);if(!product)notFound();const related=await getRelatedProducts(product.categoryId,product.id);let specs:Specs={};try{specs=typeof product.specs==="string"?JSON.parse(product.specs) as Specs:(product.specs??{}) as Specs}catch{specs={}}const specEntries=Object.entries(specs),name=localizedName(locale,product),description=localizedDescription(locale,product),categoryName=product.category?localizedName(locale,product.category):"",image=product.images?.[0];const reviews=(product.reviews??[]) as ProductReview[],validRatings=reviews.map(r=>Number(r.rating)).filter(r=>Number.isFinite(r)&&r>=1&&r<=5),avgRating=validRatings.length?validRatings.reduce((a,b)=>a+b,0)/validRatings.length:null,Arrow=locale==="fa"||locale==="ar"?ChevronLeft:ChevronRight;const hasDiscount=Number(product.compareAtPrice)>Number(product.price)&&Number(product.price)>=0,discount=hasDiscount?Math.round((1-product.price/product.compareAtPrice)*100):0;const stock=Math.max(0,Math.floor(Number(product.stock)||0)),configuredMax=product.maxOrderQty==null?null:Math.max(0,Math.floor(Number(product.maxOrderQty)||0)),purchaseMax=configuredMax===null?stock:Math.min(stock,configuredMax);const featureCards=[{icon:Weight,label:l(locale,"وزن دستگاه","Device weight","Cihaz ağırlığı","وزن الجهاز"),value:localizedSpecValue(locale,specs["وزن"]??specs["Weight"]??"—")},{icon:Volume2,label:l(locale,"سطح صدا","Noise level","Ses seviyesi","مستوى الصوت"),value:localizedSpecValue(locale,specs["سطح صدا"]??specs["Noise level"]??"—")},...(product.warrantyMonths&&product.warrantyMonths>0?[{icon:BadgeCheck,label:l(locale,"گارانتی","Warranty","Garanti","الضمان"),value:l(locale,`${product.warrantyMonths} ماه`,`${product.warrantyMonths} months`,`${product.warrantyMonths} ay`,`${product.warrantyMonths} شهر`)}]:[]),{icon:Droplets,label:l(locale,"رطوبت‌ساز","Humidifier","Nemlendirici","المرطب"),value:localizedSpecValue(locale,specs["رطوبت‌ساز"]??specs["Humidifier"]??"—")}];
 return <main className="flex-1 bg-[#f7fafd] py-5 sm:py-8 lg:py-10"><Container><nav className="mb-6 flex flex-wrap items-center gap-2 text-[11px] font-medium text-[#747780]" aria-label={l(locale,"مسیر صفحه","Breadcrumb","Sayfa yolu","مسار الصفحة")}><Link href="/" className="vitalis-focus rounded hover:text-[#001736]">{l(locale,"خانه","Home","Ana sayfa","الرئيسية")}</Link><Arrow aria-hidden="true" className="h-3.5 w-3.5"/><Link href="/shop" className="vitalis-focus rounded hover:text-[#001736]">{l(locale,"فروشگاه","Shop","Mağaza","المتجر")}</Link>{categoryName&&product.category?.slug?<><Arrow aria-hidden="true" className="h-3.5 w-3.5"/><Link href={`/shop/${product.category.slug}`} className="vitalis-focus rounded hover:text-[#001736]">{categoryName}</Link></>:null}<Arrow aria-hidden="true" className="h-3.5 w-3.5"/><strong className="max-w-[22rem] truncate text-[#181c1e]" aria-current="page">{name}</strong></nav><section className="grid gap-8 lg:grid-cols-[1.04fr_.96fr] lg:items-start lg:gap-12"><ProductGallery images={product.images??[]} locale={locale} fallbackAlt={name}/><div className="lg:pt-1"><div className="flex flex-wrap items-center gap-2">{product.brand?<span className="rounded-full border border-[#dfe3e8] bg-white px-3 py-1.5 text-[11px] font-black text-[#43474f]">{product.brand}</span>:null}{stock>0?<span className="inline-flex items-center gap-1.5 rounded-full bg-[#e2f7eb] px-3 py-1.5 text-[11px] font-black text-[#187143]"><CheckCircle2 aria-hidden="true" className="h-3.5 w-3.5"/>{t("inStock")}</span>:<span className="rounded-full bg-[#ffdada] px-3 py-1.5 text-[11px] font-black text-[#920028]">{t("outOfStock")}</span>}</div><h1 className="mt-5 text-3xl font-black leading-[1.35] text-[#001736] sm:text-4xl lg:text-[2.6rem]">{name}</h1><div className="mt-4 flex flex-wrap items-center gap-x-5 gap-y-2 text-xs text-[#747780]">{avgRating!==null?<span className="inline-flex items-center gap-1.5"><Star aria-hidden="true" className="h-4.5 w-4.5 fill-[#f4b942] text-[#f4b942]"/><strong className="text-[#181c1e]">{avgRating.toFixed(1)}</strong><span>({validRatings.length})</span></span>:null}{product.modelNumber?<span>{l(locale,"مدل","Model","Model","الموديل")}: <strong className="text-[#181c1e]">{product.modelNumber}</strong></span>:null}{product.sku?<span>SKU: <strong className="text-[#181c1e]">{product.sku}</strong></span>:null}</div>{description?<p className="mt-6 whitespace-pre-line text-sm leading-8 text-[#43474f]">{description}</p>:null}<div className="mt-7 rounded-[1.6rem] border border-[#e0e3e6] bg-white p-6 shadow-[0_20px_54px_rgba(0,23,54,.075)] sm:p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div className="tabular-nums">{hasDiscount?<div className="mb-1 text-sm font-medium text-[#9aa0aa] line-through">{formatPrice(product.compareAtPrice,locale)} {c("currency")}</div>:null}<div className="flex items-baseline gap-2"><strong className="text-3xl font-black text-[#001736] sm:text-4xl">{formatPrice(product.price,locale)}</strong><span className="text-xs font-bold text-[#747780]">{c("currency")}</span></div></div>{discount>0?<span className="rounded-xl bg-[#ba0036] px-3 py-2 text-xs font-black text-white">{discount}% {l(locale,"تخفیف","OFF","İNDİRİM","خصم")}</span>:null}</div><div className="mt-6"><AddToCartButton type="product" id={product.id} nameFa={product.nameFa} nameTr={product.nameTr} nameEn={product.nameEn} nameAr={product.nameAr} price={product.price} image={image?.url} disabled={purchaseMax<=0} maxQuantity={purchaseMax}/></div><div className="mt-3"><ProductActions productId={product.id}/></div></div><div className="mt-5 grid grid-cols-2 gap-3">{featureCards.map(({icon:Icon,label,value})=><div key={label} className="rounded-2xl border border-[#e0e3e6] bg-white p-4 shadow-[0_8px_24px_rgba(0,23,54,.04)]"><Icon aria-hidden="true" className="h-5 w-5 text-[#009dd8]"/><p className="mt-3 text-[10px] font-bold text-[#747780]">{label}</p><strong className="mt-1 block break-words text-xs leading-6 text-[#001736]">{value||"—"}</strong></div>)}</div></div></section><section className="mt-8 rounded-2xl border border-[#e0e3e6] bg-white p-5"><div className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-[#e7f5fb] text-[#002b5b]"><Headphones aria-hidden="true" className="h-5 w-5"/></span><div><h2 className="text-sm font-black text-[#001736]">{l(locale,"نیاز به راهنمایی دارید؟","Need product guidance?","Ürün desteğine mi ihtiyacınız var?","هل تحتاج إلى إرشاد حول المنتج؟")}</h2><p className="mt-1 text-xs leading-6 text-[#747780]">{l(locale,"برای بررسی مشخصات، سازگاری و انتخاب محصول مناسب با تیم پشتیبانی تماس بگیرید.","Contact support to review specifications, compatibility and product selection.","Özellikler, uyumluluk ve ürün seçimi için destek ekibiyle iletişime geçin.","تواصل مع الدعم لمراجعة المواصفات والتوافق واختيار المنتج.")}</p><Link href="/contact" className="vitalis-focus mt-2 inline-flex rounded text-xs font-black text-[#002b5b] hover:underline">{l(locale,"تماس با ما","Contact us","İletişim","اتصل بنا")}</Link></div></div></section>{specEntries.length?<section className="mt-12 sm:mt-16"><h2 className="mb-5 text-2xl font-black text-[#001736]">{t("specsTitle")}</h2><dl className="overflow-hidden rounded-3xl border border-[#e0e3e6] bg-white shadow-[0_14px_40px_rgba(0,23,54,.045)]">{specEntries.map(([key,value],index)=><div key={key} className={cnRow(index)}><dt className="font-semibold text-[#747780]">{key}</dt><dd className="font-bold text-[#181c1e]">{localizedSpecValue(locale,value)}</dd></div>)}</dl></section>:null}{reviews.length?<section className="mt-12 sm:mt-16"><div className="flex items-center justify-between gap-4"><h2 className="text-2xl font-black text-[#001736]">{l(locale,"نظر خریداران","Customer reviews","Müşteri yorumları","آراء العملاء")}</h2><span className="text-xs font-bold text-[#747780]">{reviews.length}</span></div><div className="mt-5 grid gap-4 md:grid-cols-2">{reviews.slice(0,6).map(review=><article key={review.id} className="rounded-2xl border border-[#e0e3e6] bg-white p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><strong className="text-sm text-[#001736]">{review.authorName}</strong>{Number(review.rating)>=1&&Number(review.rating)<=5?<span className="flex items-center gap-1 text-xs font-black text-[#8a6400]"><Star aria-hidden="true" className="h-4 w-4 fill-[#f4b942] text-[#f4b942]"/>{review.rating}/5</span>:null}</div>{review.title?<h3 className="mt-3 text-sm font-bold">{review.title}</h3>:null}{review.body?<p className="mt-2 text-sm leading-7 text-[#43474f]">{review.body}</p>:null}</article>)}</div></section>:null}{related.length?<section className="mt-12 sm:mt-16"><div className="mb-6 flex items-center justify-between gap-4"><h2 className="text-2xl font-black text-[#001736]">{t("relatedTitle")}</h2>{product.category?.slug?<Link href={`/shop/${product.category.slug}`} className="vitalis-focus inline-flex items-center gap-1 rounded text-xs font-black text-[#ba0036]">{l(locale,"مشاهده همه","View all","Tümünü gör","عرض الكل")}<Arrow aria-hidden="true" className="h-4 w-4"/></Link>:null}</div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{related.map(p=><ShopProductCard key={p.id} product={p}/>)}</div></section>:null}</Container></main>}
function cnRow(index:number){return `grid gap-2 px-5 py-4 text-sm sm:grid-cols-[minmax(180px,.35fr)_1fr] sm:px-6 ${index%2?"bg-[#f7fafd]":"bg-white"}`}
