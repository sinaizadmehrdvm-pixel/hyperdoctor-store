import { NextResponse } from "next/server";
import { supabaseSelect } from "@/lib/supabase-rest";

export async function GET(request:Request){const id=new URL(request.url).searchParams.get("id")?.trim()||"";if(!id||id.length>160)return NextResponse.json({variants:[]},{status:400});try{const variants=await supabaseSelect<{id:string;name:string;sku:string;price:number|null;compareAtPrice:number|null;stock:number;attributes:string}>("ProductVariant",{select:"id,name,sku,price,compareAtPrice,stock,attributes",productId:`eq.${id}`,isPublished:"eq.true",order:"createdAt.asc"});return NextResponse.json({variants},{headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300","X-Content-Type-Options":"nosniff"}});}catch(error){console.error("[product-variants] read failed",error);return NextResponse.json({variants:[]},{status:500});}}
